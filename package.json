{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna exec -- npm run build",
    "publish": "lerna run build && lerna publish",
    "test": "jest"
  },
  "devDependencies": {
    "@types/express": "^4.17.1",
    "@types/jest": "^24.0.19",
    "@types/node": "^12.11.2",
    "jest": "^24.9.0",
    "lerna": "^3.18.1",
    "ts-jest": "^24.1.0",
    "typescript": "^3.6.4"
  }
}
